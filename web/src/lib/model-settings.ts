/**
 * Model settings helpers — mirrors apps/desktop/src/app/settings/constants.ts
 * (Model section keys).
 */

/** Desktop Settings → Model config keys (below the main model picker). */
export const MODEL_CONFIG_KEYS = [
  "model_context_length",
  "fallback_providers",
] as const;
