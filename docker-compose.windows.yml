#
# docker-compose.windows.yml — Windows Docker Desktop compatible
#
# Differences from docker-compose.yml:
#   - Removes `network_mode: host` (not supported on Docker Desktop for Windows)
#   - Uses explicit port mappings instead
#   - Uses Windows-style volume path for ~/.work4you
#
# Usage:
#   docker compose -f docker-compose.windows.yml up -d
#
services:
  gateway:
    image: work4you/work4you:latest
    container_name: work4you
    restart: unless-stopped
    volumes:
      - ${USERPROFILE}/.work4you:/opt/data
    environment:
      - WORK4YOU_UID=10000
      - WORK4YOU_GID=10000
    command: ["gateway", "run"]

  dashboard:
    image: work4you/work4you:latest
    container_name: work4you-dashboard
    restart: unless-stopped
    depends_on:
      - gateway
    volumes:
      - ${USERPROFILE}/.work4you:/opt/data
    environment:
      - WORK4YOU_UID=10000
      - WORK4YOU_GID=10000
      - WORK4YOU_DASHBOARD_HOST=0.0.0.0
    ports:
      - "127.0.0.1:9119:9119"
    command: ["dashboard", "--host", "0.0.0.0", "--port", "9119", "--no-open", "--insecure"]
